server:
  port: 8888

info:
  name: "Config Service"

spring:
  application:
    name: config-service
  cloud:
    config:
      enabled: true
      server:
        git:
          uri: https://github.com/Oreste-Luci/netflix-oss-example-config-repo