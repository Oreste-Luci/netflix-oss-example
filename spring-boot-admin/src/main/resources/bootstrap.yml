#
# Use valid URI characters (eg: do not use _ in names)
#

server:
  port: 8085

spring:
  application:
    name: Spring-Boot-Admin

info:
  name: "Spring-Boot-Admin"
  version: @pom.version@
  stage: test

spring:
  application:
    name: @pom.artifactId@
  boot:
    admin:
      url: http://localhost:8080
  cloud:
    config:
      enabled: false

endpoints:
  health:
    sensitive: false